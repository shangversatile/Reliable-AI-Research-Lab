# Planned Code Structure

The following structure is a future plan only. These directories are not created during protocol initialization.

```text
configs/
data/
src/
experiments/
reports/
results/
tests/
```

Actual implementation folders will be created only after the thesis-asset audit.

Public interfaces must be chosen after reviewing old code.

No migration should happen before verifying split logic, metrics, corruption protocol, seeds, configuration traceability, and reproducibility.
